# 図脳RAPID向け DXF出力の実装ガイドラインと知見

WEBブラウザ上のJavaScriptやその他のプログラムから、国産CADソフト「図脳RAPID」で正確に読み込めるDXFファイルを出力する際に得られた知見と注意点です。特に、公共座標系のような広域かつ巨大な座標を扱う地図系アプリケーションの開発において非常に重要となります。

## 1. 文字コードと日本語テキストのエンコーディング

図脳RAPID（特にAutoCAD R12 / AC1009形式として読み込ませる場合）は、日本語テキストの扱いに独自の制約があります。

* **Unicodeエスケープ (`\U+XXXX`) は非対応**:
  最近のAutoCAD (2007以降 / AC1021) では、非ASCII文字を `\U+4FDD` のようなUnicodeエスケープシーケンスで記述できますが、図脳RAPIDで古いDXF規格としてパースされる場合、このエスケープ文字列は解釈されず、文字が消える（空文字になる）、あるいはそのまま表示されるといった不具合が発生します。
* **ネイティブなShift-JISでのバイナリ出力が必須**:
  DXFヘッダで `$DWGCODEPAGE` を `ANSI_932` (Shift-JIS) に指定するだけでなく、**ファイル自体のバイナリエンコーディングが完全にShift-JISである必要があります**。
  JavaScriptの文字列はUTF-16であり、そのまま `new Blob([text])` に渡すとUTF-8として保存されるため文字化けします。
* **解決策**: 
  `encoding-japanese` などのライブラリを使用し、DXFの文字列全体をShift-JISの `Uint8Array` に変換してからBlobを生成してダウンロードさせます。
  ```javascript
  import Encoding from 'encoding-japanese';
  
  // 文字列のDXFデータをShift-JISの配列に変換
  const sjisArray = Encoding.convert(dxfString, { to: 'SJIS', type: 'array' });
  const blob = new Blob([new Uint8Array(sjisArray)], { type: "application/dxf" });
  ```

## 2. 巨大座標（公共座標系）における縮尺と用紙サイズの自動計算の罠

公共座標系のデータは、原点(0,0)から数十km〜数百km離れた位置（例: X=70,000,000 mm）に図形が配置されます。これに対し、図脳RAPIDの「用紙サイズと縮尺をヘッダの記述から得る」機能を利用しようとすると、数学的な矛盾により自動計算が破綻します。

* **図脳RAPIDの縮尺決定ロジック**:
  図脳RAPIDは、「図形全体がDXFヘッダの図面範囲 (`$LIMMAX`) に収まっているか」をチェックします。収まっていない場合、ヘッダの用紙サイズ設定 (`$PLIMMAX`) などを無視し、強制的にA3サイズ(420mm)等のデフォルト用紙サイズに図形を収めるよう縮尺を再計算（例: 70km ÷ 420mm ≒ 1/200000 等）します。
* **1/5000を強制できない理由**:
  70km先の図形を図面範囲に収めるためには、図面範囲を 100km などに設定する必要があります。これを 1/5000 にするためには、ヘッダに記述する用紙サイズが 20,000mm（20メートル）必要になります。しかしCAD側は「20メートルの用紙サイズ」などという規格外の数値を拒否し、自動計算を放棄してしまいます。
* **解決策**:
  プログラム側からヘッダ (`$EXTMAX`, `$LIMMAX`, `$PLIMMAX`等) を操作して強制的に特定の縮尺（1/5000等）で読み込ませることは諦めます。
  図形を実際の座標（1:1）で出力した上で、**ユーザーに図脳RAPIDの読み込み設定で「用紙サイズと縮尺をヘッダから記述から得る」のチェックを外して読み込み、手動で縮尺（1/5000など）を指定させる**のが最も確実で正しいアプローチです。

## 3. 背景の白塗り（マスク）とテキストの描画順序・色指定

ラベル文字などを目立たせるために背景を白塗りの四角形でマスクする場合、`SOLID` エンティティを使用します。

* **同色による文字潰れ**:
  DXFにおいてカラーコード `7` は、背景色が黒の時は白、白の時は黒になる「White/Black」として扱われます。
  背景の `SOLID` に色 `7` を指定し、その上の `TEXT` にも色 `7` を指定した場合、画面上では黒い背景の上に黒い文字が乗ることになり、文字が完全に同化して見えなくなります（空文字になっていると錯覚しやすい）。
* **True White (255) の使用**:
  背景のマスク用途には、完全な白であるカラーコード `255` (True White) をレイヤまたはエンティティに指定します。
  これにより、白い四角の上に黒 (色番号 `7`) のテキストや枠線がくっきりと表示されます。
* **描画順序の確保**:
  DXFの `BLOCK` や通常のエンティティリストにおいて、要素は記述された順に描画されます（Z-index的な概念はありません）。
  必ず `SOLID` (背景) を先に記述し、その後に `TEXT` や `LINE` (枠線) を記述してください。

## 4. UI（画面表示）とDXF（実寸）のスケーリング分離

操作画面（ブラウザ上のSVGなど）と、実際のCAD（DXF）では求められる文字サイズが根本的に異なります。

* **CAD側の要件**: 縮尺に関わらず、印刷時に一定のサイズ（例: 1/5000の図面で3mmの文字 ＝ 実寸15メートル）になるよう、絶対的な座標系でテキストサイズを決定する必要があります。
* **画面側の要件**: 全体を見渡すためにズームアウトした場合でも、文字が小さくなりすぎず読める（あるいは重ならないように調整できる）必要があります。
* **解決策**:
  「DXF出力用の文字サイズ倍率」と「画面表示用の文字サイズ倍率」の2つの状態（State）を完全に分離します。
  UI上で文字をドラッグして位置を微調整する際、画面表示が5倍に拡大されていても、テキストのアンカー座標 (X, Y) 自体は正しい実座標を持っています。そのため、DXF出力時には「画面上の配置バランスを保ったまま、文字サイズだけが本来の規定サイズに縮小されて出力される」という理想的な挙動を実現できます。

## 5. 互換性のためのフォーマット方針

* 新しいエンティティ（例: `LWPOLYLINE`）は避け、単純な `LINE` や `POLYLINE` を使用する。
* Z座標（高度）が不要な2D図面でも、グループコード `30`, `31` などのZ値には常に `0.0` を明記しておく。
* 改行コードは `\r\n` (CRLF) に統一する。一部の古いCADソフトは `\n` だけだとパースエラーを起こす場合があります。
